const processSends = require('./processSends');
const processBurns = require('./processBurns');

function detect() {
  //Process sends
  //process burns
}

module.exports = {
  detect,
  processSends,
  processBurns,
};
